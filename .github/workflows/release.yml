name: Release

on:
  push:
    tags:
      - "v*"
    branches: [main]

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: ./.github/workflows/release_generic.yml
    with:
      snapshot: false
